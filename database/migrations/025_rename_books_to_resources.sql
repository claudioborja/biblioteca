-- 025_rename_books_to_resources.sql
-- Legacy compatibility migration.
-- In this codebase, the schema already uses resources/users naming,
-- so there is nothing to rename.
SET @migration_025_skipped := 1;
